# src/main/resources/application.properties

# Server Port
server.port=8080

# H2 Database Configuration for main application run
spring.datasource.url=jdbc:h2:mem:marriagebureau;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA/Hibernate Configuration for main application: Flyway manages schema
spring.jpa.hibernate.ddl-auto=none 
# Flyway will handle schema changes
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# H2 Console Configuration
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Flyway Migration Configuration
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

# JWT Configuration
app.jwtSecret=FHT+35LrJninBmCRjL5K2vKC+mk4ndlS50HrxL5JELogEvadzyE+EQ8Et+AXOxJWKClZlmkVwVxJvCRxO8gm/w==
app.jwt-expiration-milliseconds=86400000
# 24 hours in milliseconds (24 * 60 * 60 * 1000)

# Logging (as desired for main app)
logging.level.org.springframework.security=DEBUG 
logging.level.org.springframework.security.web=TRACE
logging.level.org.springframework.security.authentication=TRACE
logging.level.com.marriagebureau=DEBUG
# Keep DEBUG for now to see full security chain
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql=TRACE 
# TRACE for parameter binding

# application.properties or application.yml
# Replace with your actual OIDC Issuer URI (e.g., Keycloak, Auth0, Okta, Azure AD)
# spring.security.oauth2.resourceserver.jwt.issuer-uri=https://your-oidc-provider.com/auth/realms/your-realm