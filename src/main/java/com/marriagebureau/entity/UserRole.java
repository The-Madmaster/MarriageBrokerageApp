// src/main/java/com/marriagebureau/entity/UserRole.java
package com.marriagebureau.entity;

public enum UserRole {
    ADMIN,
    BROKER,
    USER
    // Add other roles if needed, e.g., MEMBER if members can log in directly
}