    // src/main/java/com/marriagebureau/user/Role.java
    package com.marriagebureau.user;

    // Defines the roles a user can have in the application.
    // This simple enum can be extended with more roles as needed.
    public enum Role {
        USER,
        ADMIN
    }
    